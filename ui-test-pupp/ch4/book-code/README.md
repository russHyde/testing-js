# Chapter 4 - firebase shopping cart example

Installed deps using `npm install`
This warned re security vulnerabilities in some dependencies
Therefore ran `npm audit` and `npm audit fix` to sort problems out

